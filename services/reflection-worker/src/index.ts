// reflection-worker — очередь LLM-рефлексий (ТЗ А.1, 7.3).
// Фаза 1 «Каркас»: заготовка процесса.
//
// ВАЖНО (CLAUDE.md, «Незыблемые правила», п.7): боевую симуляцию с реальными
// LLM-вызовами запрещено запускать до фазы 6 — до этого рефлексия только
// замокана. Ключ ANTHROPIC_API_KEY читается исключительно из окружения
// сервера и никогда не должен попадать во фронтенд (п.2).

console.log("[reflection-worker] заготовка процесса запущена (фаза 1 «Каркас»)");

process.on("SIGTERM", () => {
  console.log("[reflection-worker] получен SIGTERM, завершение");
  process.exit(0);
});

setInterval(() => {
  // Плейсхолдер очереди рефлексий — реализуется в фазе 5+ (сначала замокано).
}, 1000 * 60 * 60);
