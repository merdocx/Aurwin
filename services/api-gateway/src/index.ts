// api-gateway — WebSocket-поток состояния + REST для карточек (ТЗ А.1, 7.1).
// Фаза 1 «Каркас»: заготовка процесса. api-gateway — только читатель БД,
// никогда не пишет игровое состояние (А.1). Аутентификации нет — публичный
// read-only поток (7.1).

console.log("[api-gateway] заготовка процесса запущена (фаза 1 «Каркас»)");

process.on("SIGTERM", () => {
  console.log("[api-gateway] получен SIGTERM, завершение");
  process.exit(0);
});

setInterval(() => {
  // Плейсхолдер HTTP/WebSocket-сервера — реализуется в следующих фазах.
}, 1000 * 60 * 60);
