#!/usr/bin/env bash
# Aurwin: создание обвязки проекта (Приложение Б ТЗ).
# Запускать от пользователя aurwin:  bash aurwin-bootstrap.sh
# Идемпотентен: существующие файлы не перезаписывает.
set -euo pipefail

ROOT=/opt/aurwin
mkdir -p "$ROOT"/{docs,ops/phases,ops/reports,ops/logs,src}
cd "$ROOT"

write() {  # write <path> <<'EOF' ... EOF
  local path="$1"
  if [ -e "$path" ]; then
    echo "  пропуск (уже есть): $path"
    cat > /dev/null
  else
    cat > "$path"
    echo "  создан: $path"
  fi
}

echo "== CLAUDE.md =="
write CLAUDE.md <<'EOF'
# Aurwin — контекст проекта

Полное ТЗ: docs/AURWIN_TZ.md. Читай его перед любой задачей.
При конфликте твоего решения с ТЗ — прав ТЗ; расхождения фиксируй в ops/DEVIATIONS.md.

## Незыблемые правила
1. Никогда не коммить .env и любые ключи. Секреты читать только из окружения.
2. Ключ Anthropic не должен попадать во фронтенд-код ни в каком виде.
3. Все константы симуляции — только в config/constants.yaml по таблице А.9.
   Не хардкодить числа в коде.
4. Не менять значения констант «чтобы тест прошёл». Тест не проходит — чинить логику
   или писать в ops/DEVIATIONS.md с обоснованием.
5. Симуляция никогда не блокируется на LLM-вызове (раздел 7.3).
6. Не удалять и не пересоздавать БД без явного указания. Данные мира — ценность.
7. Не запускать боевую симуляцию с реальными LLM-вызовами до фазы 6.
   До этого рефлексия только замокана.
8. Действия «обмануть» не существует. Сигнал — обычное действие; ложность
   вычисляется постфактум как расхождение true_state и claimed_state (7.8).
9. Численность популяции — не настраиваемый параметр. Не устраивает численность —
   меняй ёмкость среды (респавн рыбы, летальность охоты), а не число особей (7.4).

## Стек (раздел 7.5)
Node.js/TypeScript, PostgreSQL, WebSocket, React + PixiJS, docker-compose.

## Определение готовности фазы
Фаза завершена, когда `npm test` зелёный, `docker compose up` поднимается без ошибок,
и в ops/reports/phase-N.md записано: что сделано, что не сделано, чем проверено.
EOF

echo "== промпты фаз =="

write ops/phases/phase-1.md <<'EOF'
Задача: фаза 1 «Каркас» проекта Aurwin.

Прочитай docs/AURWIN_TZ.md, разделы 7.1, 7.5, А.1, А.7, А.9.

Создай:
- структуру репозитория под компоненты А.1 (sim-engine, reflection-worker,
  api-gateway, frontend) в монорепозитории с общим package.json;
- config/constants.yaml — ВСЕ константы из таблицы А.9 с комментариями-обоснованиями;
- docker-compose.yml: postgres (без публикации порта наружу), заготовки сервисов;
- .gitignore, обязательно включающий .env;
- npm test (jest или vitest) — пока с одним smoke-тестом, читающим constants.yaml
  и проверяющим, что все ожидаемые ключи присутствуют;
- README.md с командами запуска;
- git init и первый коммит.

Гейт: `docker compose up -d` поднимает postgres, `npm test` зелёный,
`git status` чист, .env отсутствует в индексе.

По завершении: отчёт в ops/reports/phase-1.md.
EOF

write ops/phases/phase-2.md <<'EOF'
Задача: фаза 2 «Данные» проекта Aurwin.

Прочитай docs/AURWIN_TZ.md, раздел А.2 целиком.

Реализуй систему миграций и создай ВСЕ таблицы из А.2:
creatures, episodes, bonds, aversions, trait_history, perceived_states,
perceived_zone_threat, signal_trust, signals, reflections, world_events, decision_log.

Критично:
- инвариант bonds: CHECK (creature_a < creature_b) — пара не должна попадать дважды;
- aversions направленные (subject_id -> object_id), НЕ симметричные;
- поля creatures из А.2 полностью: traits (включая expressiveness), traits_birth,
  skills, habits, weights, weights_birth, chronotype, is_asleep, authority,
  last_reflection_at, needs (включая sleep_pressure);
- episodes: learned_from, transmission_depth;
- политика ретенции из А.2 — как отдельные функции/скрипты обслуживания
  (прунинг памяти, TTL decision_log, обнуление request/response в reflections и т.д.).

Тесты обязательны: попытка вставить bonds с creature_a > creature_b должна падать;
миграции применяются и откатываются.

Гейт: миграции up/down проходят, тесты зелёные.

По завершении: отчёт в ops/reports/phase-2.md.
EOF

write ops/phases/phase-3.md <<'EOF'
Задача: фаза 3 «Мир» проекта Aurwin.

Прочитай docs/AURWIN_TZ.md, разделы 6.2, А.10.

Реализуй:
- карту 2000x1200 у.е. с пятью именованными зонами из А.10 и их долями;
- плотность рыбы по кормовым зонам с респавном и локальным истощением;
- суточный цикл: внутренние сутки ~3.4 реальных часа, день/ночь по половине,
  ночные модификаторы восприятия и доступности рыбы (7.10);
- движение существ со скоростями из А.10, отражение от границ карты;
- пространственный индекс для поиска соседей в радиусе восприятия
  (он понадобится в фазе 4 и определяет производительность — цель 5).

Тесты: рыба восстанавливается до потолка и истощается локально; существо
не покидает карту; ночью радиус восприятия уменьшается согласно константам.

Гейт: npm test зелёный.

По завершении: отчёт в ops/reports/phase-3.md.
EOF

write ops/phases/phase-4.md <<'EOF'
Задача: фаза 4 «Жизнь» проекта Aurwin. Самая большая фаза — не торопись.

Прочитай docs/AURWIN_TZ.md, разделы 6.2, 7.4, 7.7, 7.8, 7.9, 7.10, А.3, А.4, А.9, А.10.

Реализуй:
- тик-пайплайн по А.3 (все 14 шагов, визуальный тик 2 сек, биочасы x7);
- utility AI по А.4 со всеми слагаемыми, включая навыки и привычки;
  веса индивидуальны для каждой особи (7.7); факторы решения пишутся
  в decision_log с семплингом из А.2;
- охота по вероятностной модели А.10 со всеми модификаторами и влиянием навыка;
- размножение по 7.4: разнополость, возрастная стадия, запрет инбридинга
  на глубину 2 поколений;
- возрастные стадии по А.10, включая неэффективное самокормление детёнышей (x0.3);
- механики развития поведения 7.7: рост и затухание навыков, личные оценки зон,
  социальное обучение, вертикальную передачу знаний (взвешивание авторитетностью
  источника, transmission_depth);
- сигнальную систему 7.8: разделение истинного и воспринимаемого состояния,
  display_vigor / alarm_call / stealth_approach, выбор цели касаткой по
  воспринимаемой бодрости, кредит доверия, черта expressiveness;
- родство и кооперацию 7.9: guard_offspring, provision, coordinate_hunt;
  навык parenting должен реально использоваться этими действиями;
- сон 7.10: давление сна, действие sleep, хронотип, полусон касаток,
  пробуждение по alarm_call, штрафы усталости;
- метрики: поведенческая дивергенция когорт, сигнальная система, традиции
  (доля эпизодов с transmission_depth >= 2), сон.

КРИТИЧНО по 7.8: не создавай действие «обмануть» и не помечай сигнал как ложный
в момент подачи. Сигнал — обычное действие в формуле U(a). Лживость вычисляется
постфактум. Если возникнет соблазн добавить флаг is_deceptive — это ошибка
проектирования, напиши об этом в ops/DEVIATIONS.md вместо реализации.

LLM-рефлексию НЕ подключай: заглушка, возвращающая фиксированные намерения.
Настоящая рефлексия — фаза 6. Дрейф весов через рефлексию тоже отложи,
но структуру данных под него заложи.

Обязательно: `npm run simulate -- --days 30 --fast` — прогон без задержек
реального времени, с отчётом по популяции, дисперсии черт, поведенческой
дивергенции и сигнальной статистике. Поддержи флаг --seed для воспроизводимости.

Гейт: прогон 30 суток завершается, ни один вид не вымер, дисперсия черт
не упала более чем вдвое, поведенческая дивергенция когорты выросла к зрелости,
сигналы подаются и измеримо влияют на выбор цели касаткой, доля спящих
ночью никогда не достигает 100%.

Уровень нечестности сигналов может оказаться любым, включая нулевой, —
это законный исход отбора и НЕ повод подкручивать параметры (7.11).
Численность популяции НЕ настраивается напрямую: не устраивает — меняй
ёмкость среды, а не число особей.

Если баланс не сходится — подбирай константы респавна рыбы и вероятности охоты
в config/constants.yaml, каждое изменение записывай в ops/BALANCE_LOG.md
с указанием, что именно изменилось в динамике.

По завершении: отчёт в ops/reports/phase-4.md.
EOF

write ops/phases/phase-5.md <<'EOF'
Задача: фаза 5 «Наблюдение» проекта Aurwin.

Прочитай docs/AURWIN_TZ.md, разделы 5, 6.1 (блок «Наблюдение»), А.6.

Реализуй api-gateway:
- WebSocket: snapshot при подключении, delta каждые 1–2 сек, только по видимой
  области клиента;
- REST: /api/creatures/{id}, /api/social-graph, /api/world/stats;
- ЗАПРЕТ: поле narrative не отдаётся наружу ни одним публичным эндпоинтом,
  только narrative_facts;
- лимиты из А.6: 60 REST-запросов/мин с IP, 3 WS-соединения с IP,
  кэш social-graph 10 сек, graceful-отказ при перегрузке.

Реализуй фронтенд (React + PixiJS):
- рендер мира с интерполяцией движения между дельтами;
- визуальная смена дня и ночи (освещение сцены);
- спящие существа визуально отличимы от бодрствующих;
- сигналы и тревоги отображаются как заметные события на карте;
- индикаторы эмоций на существах;
- карточка существа: имя, вид, возраст, черты, ведущие навыки, состояние сна,
  narrative_facts, таймлайн событий;
- социальная карта (граф дружб);
- без регистрации и без каких-либо действий, кроме навигации и приближения.

Гейт: клиент получает дельты и рендерит; ночь визуально отличима от дня;
видно, кто спит; alarm_call заметен; narrative отсутствует в ответах публичных
эндпоинтов (проверить тестом); rate-limit срабатывает.

По завершении: отчёт в ops/reports/phase-5.md.
EOF

write ops/phases/phase-6.md <<'EOF'
Задача: фаза 6 «Сознание» проекта Aurwin. С этой фазы начинаются реальные траты по ключу.

Прочитай docs/AURWIN_TZ.md, разделы 7.3, 7.6, 7.8.6, А.5.

Реализуй reflection-worker:
- очередь рефлексий, обработка строго вне тикового цикла;
- фоновая рефлексия раз в 24 реальных часа, привязанная к моменту сна существа
  (7.10), через Batch API;
- событийная рефлексия с дебаунсом 4 ч на существо, слиянием событий окна
  и глобальными лимитами 30/час И 120/сутки;
- промпт-кэширование общей системной части;
- модели: Haiku для фоновой, Sonnet для событийной (сверить актуальные ID
  и цены в документации Anthropic перед реализацией);
- контракт вход/выход строго по А.5, включая bonds, aversions, habits, skills,
  signal_history, was_deceived_by;
- применение результата: дельты черт с clamp ±0.1 и пожизненным коридором ±0.6,
  дельты весов ±0.05 с коридором ±0.3, намерения, narrative, narrative_facts;
- обработка смерти с «висящей» рефлексией: результат отбрасывается, текст
  сохраняется как «последняя мысль»;
- деградация при недоступности API: симуляция не замедляется, очередь копится
  с экспоненциальными ретраями, пропущенные сутки не навёрстываются кратно;
- валидация выхода: JSON-схема, длина narrative, диапазоны дельт, белый список
  эффектов, ссылки только на существующие ID;
- ЧЕСТНОСТЬ ПОВЕСТВОВАНИЯ (7.8.6): системный промпт запрещает описывать события,
  отсутствующие во входных данных. Добавь тест, сверяющий narrative_facts
  с журналами signals/episodes/world_events.

Перед первым боевым запуском убедись, что ANTHROPIC_API_KEY читается из окружения
и нигде не логируется.

Гейт: интеграционный тест на 5–10 существах — полный цикл эпизод -> рефлексия ->
изменение поведения; тест деградации при недоступном API; тест честности
повествования.

По завершении: отчёт в ops/reports/phase-6.md, включая фактическую стоимость
тестовых прогонов.
EOF

write ops/phases/phase-7.md <<'EOF'
Задача: фаза 7 «Эксплуатация» проекта Aurwin.

Прочитай docs/AURWIN_TZ.md, разделы 6.1 (блок наблюдаемости), А.7, А.9.

Реализуй:
- экспорт метрик (Prometheus) и дашборд (Grafana в docker-compose):
  популяция по видам, рождения/смерти по причинам, продолжительность жизни,
  поведенческая дивергенция, распределение навыков, метрики сигнальной системы,
  традиций и сна, метрики LLM (вызовы, стоимость, латентность, ошибки);
- алёрты из А.7 с видоспецифичными порогами: остановка тика > 2 мин,
  ошибки LLM > 50%/час, пингвины вне 10–120, касатки вне 2–12,
  расход LLM > 2x планового (~$0.40/сутки);
- systemd-юниты для sim-engine, reflection-worker, api-gateway с автостартом;
- снапшот мира не реже раза в минуту, восстановление из снапшота при рестарте,
  правило «время после паузы НЕ догоняется» (А.7);
- бэкапы Postgres (pg_dump по расписанию + WAL-архивация);
- скрипты обслуживания ретенции по расписанию;
- README по эксплуатации: как смотреть метрики, как перезапустить, как откатиться.

Гейт: перезагрузка сервера -> мир поднимается из снапшота автоматически и
продолжает тикать; алёрт срабатывает на искусственно заниженной популяции;
бэкап создаётся и восстанавливается.

По завершении: отчёт в ops/reports/phase-7.md.
EOF

echo "== build.sh =="
write ops/build.sh <<'EOF'
#!/usr/bin/env bash
# Последовательный прогон фаз с независимыми гейтами (Приложение Б).
# Использование:  bash ops/build.sh [первая_фаза]   (по умолчанию с 1)
set -euo pipefail
cd /opt/aurwin
mkdir -p ops/reports ops/logs

START="${1:-1}"

for phase in $(seq "$START" 7); do
  echo "=== Фаза $phase — старт $(date '+%F %T') ==="

  claude -p "$(cat ops/phases/phase-${phase}.md)" \
    --allowedTools "Read,Edit,Write,Bash,Grep,Glob" \
    --permission-mode acceptEdits \
    --max-turns 150 \
    --output-format json \
    > "ops/logs/phase-${phase}.json" \
    2> "ops/logs/phase-${phase}.err" || {
      echo "Фаза $phase: агент завершился с ошибкой. Смотри ops/logs/phase-${phase}.err"
      exit 1; }

  cost=$(jq -r '.total_cost_usd // "n/a"' "ops/logs/phase-${phase}.json" 2>/dev/null || echo "n/a")
  echo "Стоимость фазы $phase: \$${cost}"

  # Гейт проверяется независимо от отчёта агента
  if ! npm test; then
    echo "Фаза $phase: гейт не пройден (npm test). Стоп."
    echo "Починить и повторить:  bash ops/build.sh $phase"
    exit 1
  fi

  git add -A && git commit -m "phase ${phase}: complete" || true
  echo "=== Фаза $phase пройдена $(date '+%F %T') ==="
done

echo "Сборка завершена. Отчёты: ops/reports/"
EOF
chmod +x ops/build.sh 2>/dev/null || true

touch ops/DEVIATIONS.md ops/BALANCE_LOG.md

echo
echo "Готово. Структура создана в $ROOT"
echo
echo "Осталось вручную:"
echo "  1. Положить ТЗ в $ROOT/docs/AURWIN_TZ.md"
echo "  2. Создать $ROOT/.env с ANTHROPIC_API_KEY и POSTGRES_PASSWORD (chmod 600)"
echo "  3. Авторизовать Claude Code:  claude  -> /login"
echo "  4. Запустить:  tmux new -s build 'bash ops/build.sh 2>&1 | tee ops/logs/build.log'"
