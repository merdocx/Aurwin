# Отчёт по фазе 4 «Жизнь»

Дата: 2026-07-31

## Статус гейта

Полный прогон `npm run simulate -- --days 30 --fast --seed 1` **не завершён**:
оба запуска прерваны (`EXIT:143` / Terminated) при освобождении RAM под
публичный стек и приоритетные задачи. Первый незафиксированный прогон также
ловил OOM (`EXIT:137`) до фикса drain-буферов в CLI.

Зафиксирован успешный короткий прогон:

```
npm run simulate -- --days 1 --fast --seed 1
```

(~106 с, 409 тик/с, отчёт в `/tmp/aurwin-sim-1d.txt`).

## Что сделано в фазе 4 (код)

- Тик-пайплайн А.3, utility AI А.4, охота/размножение/сон/сигналы/родство
  (см. `services/sim-engine/src/sim/`, коммит `phase 4: complete`).
- CLI `npm run simulate -- --days N --fast --seed S`.
- Балансировка констант — `ops/BALANCE_LOG.md` (hunt 0.35→0.15→0.20→0.25,
  fish respawn снижен; кулдаун/contact radius охоты).
- Follow-up: CLI drain буферов episodes/signals/decision_log каждый тик
  (иначе 30-суточный `--fast` съедает RAM на drain без Postgres).

## Результаты `--days 1 --seed 1` (гейт частично)

| Критерий | Результат |
|---|---|
| Ни один вид не вымер | OK (пингвины 40→62, касатки 4→4) |
| Дисперсия черт не упала >2× | OK (мин. отношение ~0.74) |
| Поведенческая дивергенция когорт выросла | н/д (мало данных за 1 сутки) |
| Сигналы влияют на выбор цели | н/д (нет display_vigor при охоте за 1д) |
| Доля спящих ночью никогда 100% | OK (max ночь 0.818) |
| Тиков вне алёртов популяции | 0 / 43200 |

Сигналы за сутки: `alarm_call` 162 (опровергнуто 85), `stealth_approach` 3.

## Не закрыто

- Полный 30-суточный гейт (цель 1 MVP) — повторить на спокойном хосте /
  с swap, без параллельной нагрузки.
- Live-мир всё ещё выше целевого диапазона 30–80 / 3–8 (двойной genesis +
  рост); константы уже ужесточены, эффект на live нужно мониторить.

## Чем проверено

- `npm test` зелёный на момент follow-up (352 теста после signals persistence).
- `/tmp/aurwin-sim-1d.txt` — полный текстовый отчёт 1-суточного прогона.
